{
  "name": "task-manager-mi-app",
  "private": true,
  "scripts": {
    "dev": "concurrently -n back,front -c blue,green \"npm run dev:back\" \"npm run dev:front\"",
    "dev:back": "cd backend && npm run start:dev",
    "dev:front": "cd frontend && npm run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
